# Phaser Prototypes

This track now follows the standard Phaser web workflow with `npm`, `Vite`, and `TypeScript`.

Main flow:

```bash
cd v1-code-arena
npm install
npm run dev
```

Optional repo-level wrappers:

```bash
make install-phaser-v1
make run-phaser-v1
make build-phaser-v1
```
