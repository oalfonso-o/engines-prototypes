# Plans

This folder contains implementation plans that are more concrete than the high-level design docs and more durable than ad-hoc chat context.

Current plans:

- `3d-heading-locked-prototype-plan.md`: migration plan for a 3D prototype that preserves the current low-angle `heading_locked` feel while replacing the fragile fake 2.5D renderer.
